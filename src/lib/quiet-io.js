/* import { Socket } from 'node:net';

// Not perfect, but silence console.log and console.error
// in tests.

Object.defineProperties(globalThis.process, {
  stderr: { value: new Socket() },
  stdout: { value: new Socket() },
});
 */
